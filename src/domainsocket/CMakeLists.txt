file(GLOB_RECURSE DOMAINSOCKET_SOURCES CONFIGURE_DEPENDS *.cc)

add_library(domainsocket OBJECT ${DOMAINSOCKET_SOURCES})
add_library(ds SHARED ${DOMAINSOCKET_SOURCES})

set_target_properties(domainsocket PROPERTIES POSITION_INDEPENDENT_CODE ON)